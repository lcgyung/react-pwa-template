# docs/plans

여러 스텝·슬라이스에 걸친 기능의 **실행 계획 아티팩트**를 보관한다. plan mode로 승인된 계획을
`YYYY-MM-DD-<feature>.md` 로 기록한다. 작성 규약은 `.claude/skills/writing-plans` 참조.

- **ADR(`docs/adr/`)과 역할 분리** — ADR은 "왜"(되돌리기 어려운 설계 결정), 여기는 "무엇을 / 어떻게"(실행 계획).
- 계획은 실행하며 task 상태(`[ ]` → `[x]`)를 갱신해 진행을 추적한다.
