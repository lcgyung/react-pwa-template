# dependencies
node_modules
.pnpm-store

# build output
dist
dev-dist
storybook-static
coverage
*.local
*.tsbuildinfo

# env (commit .env.example / .env.development / .env.production; keep secrets out)
.env
.env.local
.env.*.local

# e2e (playwright)
test-results
playwright-report
/playwright/.cache
blob-report

# lighthouse ci (local run artifacts)
.lighthouseci

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
