allowBuilds:
  esbuild: true
  msw: true
# 공급망 보안 — 갓 게시된(잠재 탈취) 버전 설치를 지연한다. pnpm 11 기본값(1440분=1일)을 명시 고정해
# dependabot cooldown(.github/dependabot.yml)과의 정합을 코드로 드러낸다(pnpm 10- 는 기본 0 이라 버전
# 의존 묵시값을 신뢰하지 않는다).
#
# 불변식: 이 gate(1440분=1일) ≤ min(dependabot cooldown days)=semver-patch-days(2일). 이 값을 올리면
# dependabot cooldown 최저값도 함께 올려야 dependabot 제안이 게이트에 안 걸린다(.github/dependabot.yml 참조).
# 주의: gate 는 dependabot 뿐 아니라 모든 install(수동 범프·CI 포함)에 적용된다. 24h 미만 버전을 수동으로
# 올리면 CI install 이 ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION 으로 막힌다 — 운영 규약은
# .claude/rules/security.md '공급망 — 의존성 유휴기간' 참조. 신뢰·긴급 패키지는 minimumReleaseAgeExclude 로 한시 예외.
minimumReleaseAge: 1440
minimumReleaseAgeExclude:
  - eslint-plugin-better-tailwindcss@4.6.0
onlyBuiltDependencies:
  - esbuild
  - msw
# axios > form-data 가 4.0.5 를 끌어오는데 4.0.x 라인의 high CVE(GHSA-hmw2-7cc7-3qxx, CRLF injection)
# 패치는 4.0.6+. axios 의 form-data ^4.0.0 범위와 호환되므로 patched 라인으로 고정한다(CI pnpm audit 차단 해소).
overrides:
  form-data: ^4.0.6
  # eslint-plugin-react-hooks@7 은 zod-validation-error 의 './v4' 서브패스를 import 하는데, 이 서브패스는
  # 4.x 라인에만 있다. admin-template(zod 3)은 트리가 4.0.2 로 dedup 돼 자연 통과하지만, 이 repo(zod 4)는
  # pnpm 이 3.5.4(='./v4' 없음)로 떨어져 ERR_PACKAGE_PATH_NOT_EXPORTED 로 lint 가 크래시한다. 4.x 라인으로
  # 고정해 admin 과 동일 해석을 강제한다(4.0.2 peer: zod ^3.25 || ^4.0 — zod 4.4.3 호환).
  zod-validation-error: ^4.0.2
